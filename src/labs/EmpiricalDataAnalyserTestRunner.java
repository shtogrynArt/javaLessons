package labs;

import com.sun.net.httpserver.Authenticator;

public class EmpiricalDataAnalyserTestRunner {

    public static void main(String[] args) {
        /*Result result = JUnitCore.runClasses(EmpiricalDataAnalyserTest.class);
        for (Authenticator.Failure failure : result.getFailures())
            System.out.println(failure.toString());
        System.out.println("Test has been completed"
                + (result.wasSuccessful() ? " successfully" : " with failures"));*/
    }
}

